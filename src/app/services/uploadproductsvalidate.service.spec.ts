/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { UploadproductsvalidateService } from './uploadproductsvalidate.service';

describe('UploadproductsvalidateService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [UploadproductsvalidateService]
    });
  });

  it('should ...', inject([UploadproductsvalidateService], (service: UploadproductsvalidateService) => {
    expect(service).toBeTruthy();
  }));
});
